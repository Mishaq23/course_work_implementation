train:
  _target_: src.datasets.FakeAVCelebDataset
  root_dir: ${dataset_root}
  index_dir: ${index_dir}
  name: train
  num_frames: ${num_frames}
  train_ratio: 0.8
  val_ratio: 0.1
  test_ratio: 0.1
  split_seed: 42
  instance_transforms: ${transforms.instance_transforms.train}

val:
  _target_: src.datasets.FakeAVCelebDataset
  root_dir: ${dataset_root}
  index_dir: ${index_dir}
  name: val
  num_frames: ${num_frames}
  train_ratio: 0.8
  val_ratio: 0.1
  test_ratio: 0.1
  split_seed: 42
  instance_transforms: ${transforms.instance_transforms.inference}

test:
  _target_: src.datasets.FakeAVCelebDataset
  root_dir: ${dataset_root}
  index_dir: ${index_dir}
  name: test
  num_frames: ${num_frames}
  train_ratio: 0.8
  val_ratio: 0.1
  test_ratio: 0.1
  split_seed: 42
  instance_transforms: ${transforms.instance_transforms.inference}