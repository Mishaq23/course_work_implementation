defaults:
  - baseline
  - override /model: video_only_model
  - override /datasets: features
  - _self_

writer:
  run_name: video_pretrain

trainer:
  device_tensors: ["video", "labels"]
  monitor: "max val_AUROC"
